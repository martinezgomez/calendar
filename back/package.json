{
  "name": "calendar-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 5000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.16.2"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
